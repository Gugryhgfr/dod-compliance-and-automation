version: 1
display_name: VCFB-9X-000004 - The VMware Cloud Foundation SDDC Manager NGINX server must implement HTTP Strict Transport Security (HSTS) to protect the integrity of remote sessions of external server contexts.
global_description: |-
  HTTP Strict Transport Security (HSTS) instructs web browsers to only use secure connections for all future requests when communicating with a web site. Doing so helps prevent SSL protocol attacks, SSL stripping, cookie hijacking, and other attempts to circumvent SSL protection.
osfinger:
  - VMware Photon OS-5
refs:
  disa_stig:
    - VCFB-9X-000004
  srg:
    - SRG-APP-000015-WSR-000014
  cci:
    - CCI-001453
  nist:
    - AC-17 (2)
benchmark_id:
  vcf9_sddc_manager_nginx:
    type: custom
    desc: The VMware Cloud Foundation SDDC Manager NGINX server must implement HTTP Strict Transport Security (HSTS) to protect the integrity of remote sessions of external server contexts.
    control_id: VCFB-9X-000004
    scored: true
    profile:
      - default
    information: |-
      Verify a header is present to configure HSTS.

      View the running configuration by running the following command:

      # nginx -T 2>&1 | sed -n '/\sserver\s{/{:a;N;/.*location/!ba;/.*listen.*ssl/p}' | grep Strict-Transport-Security

      Example configuration:

      server {
      add_header Strict-Transport-Security max-age=15768000;
      }

      If an HSTS header is not configured for all servers and locations with SSL enabled, this is a finding.

      Note: There can be several add_header directives. These directives are inherited from the previous configuration level if and only if there are no add_header directives defined on the current level so care must be taken if add_header is defined at multiple levels to include headers configured at a higher level.
    rationale: |-
      HTTP Strict Transport Security (HSTS) instructs web browsers to only use secure connections for all future requests when communicating with a web site. Doing so helps prevent SSL protocol attacks, SSL stripping, cookie hijacking, and other attempts to circumvent SSL protection.
    remediation: |-
      Navigate to and open:

      The nginx.conf (/etc/nginx/nginx.conf by default or the included file where the server is defined) file.

      Add a add_header directive, for example:

      add_header Strict-Transport-Security "max-age=31536000";

      Reload the NGINX configuration by running the following command:

      # nginx -s reload

      Note: There can be several add_header directives. These directives are inherited from the previous configuration level if and only if there are no add_header directives defined on the current level so care must be taken if add_header is defined at multiple levels to include headers configured at a higher level.
